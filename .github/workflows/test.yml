name: Go Test
on:
    push:
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v5
            - uses: actions/setup-go@v6
              with:
                go-version: '1.22'
            - run: go test go/excellent/*.go
            - run: go test go/test/*.go
